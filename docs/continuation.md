# Continuation handoff

Research is paused. No process should be assumed live.
The following records preserve the current state for a future continuation.

## First read

1. `README.md`, `docs/problem.md`, `docs/results.md`.
2. Run the portable exact checks.
3. Consult the source/record manifests before interpreting historical filenames.

Do not spend another budget repeating the U₂ certificate, all triples of the
315-core, the completed F9 computation, or the closed fixed-155/radius-24 region
without a specific verification reason.

## Last local search checkpoint

The latest refined model concerns the exact fixed **115** words and parent
stored in `CF-core200-radius-cut-sep22.json`, with deletion radius **24**.

- Universe of free candidates: **6,796**.
- Search source: `search_core_ipm_branches_v12.py`.
- Last tree: `CF-core200-r24-refined-resume1-sep22.json`.
- Completed snapshot: **256 nodes**, **5 pending branches**, **1 unknown** at index 255.
- Leaf generator: `certify_core_ipm_leaves_dual_v10.py`.
- Checker: `check_core_branch_certificates_v8.py`.
- Latest proof: `CF-core200-r24-refined-prefix256-verification-sep22.json`;
  **125 integer leaves verified**, minimum positive gap **20**.
- Whole-domain exclusion: **false**.
- Original model SHA-256:
  `2810765e01072060ba6e692807b25939f29f7cac6ffbd1f15669fbe22c4d2382`.
- Strengthened target model SHA-256:
  `fb077de3307db2f5f07decfe2311cb0f97a2edbf30a89ba2b3bb07dd3239db6f`.

The row order includes 59 earlier learned clauses and then seven refined
clauses, before the three final radius/cardinality rows. Resume only under the
same model. Never reinterpret older row-indexed multipliers under a new matrix.
The v12 checkpoint keeps the previous 62 completed decisions; the earlier
unfinished leaf was retried. A future continuation must audit this preservation
and the branch partition rather than accepting counters alone.

These records are included under `archive/records/`. Original files and larger
input artifacts remain in the retained local research archive. On another
machine, first resolve the archival path/hash portability limitations; do not
silently disable checks and call the result verified.

## Questions that could justify a new approach

### A global method needs information beyond the tested pair constraints

The rational weight-368 point is a concrete obstruction. A proposed new
inequality should first be checked for validity on actual independent sets and
then evaluated against this point. Higher-order moment constraints, such as
triple consistency, are an idea to investigate; no such extension has been
implemented or shown to separate 368 here.

### A construction should escape the tested cores or small neighborhoods

The overlap result permits a 368-word set to discard four or many more words
of the selected core. A constructive method could deliberately explore such
regions instead of repeatedly perturbing the same local maxima. This is a
research suggestion, not evidence that those regions contain a solution.

### A local proof needs a global reduction before it answers the main question

Closing the last six unresolved parts of the current tree would settle that
fixed-core/radius domain. It would still leave the global question open.
An argument that every hypothetical 368 set can be transformed into a checked
domain would be substantially different progress. No such argument is known
from this work.

## Reporting rules

- A 368 witness must be explicit and pass all 67,528 unordered pair checks.
- A new 367 set must be classified before claiming a new family.
- State the exact retained core, parent, radius and library for local exclusions.
- Report timeouts and incomplete enumeration as open.
- Keep exact certificates distinct from numerical LP statuses and acknowledgments.
- Preserve source/certificate hashes when extending the archive.
